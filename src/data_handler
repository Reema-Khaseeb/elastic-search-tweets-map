from typing import Optional
 
from fastapi import FastAPI
from pydantic import BaseModel
 
class Query(BaseModel):
    text_form: str
    start_date: str # Optional[str] = None
    end_date: str # Optional[str] = None
    coor1: float
    coor2: float
    coor3: float
    coor4: float